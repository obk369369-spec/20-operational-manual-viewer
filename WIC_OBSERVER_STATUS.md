# WIC OBSERVER STATUS

최종 갱신: 2026-08-11 11:20 KST
상태: ACTIVE
목적: 링크 하나에서 각 도구의 현재 상태·개선된 부분·막힌 부분·다음 작업을 바로 이해할 수 있게 보여주는 사람용 관찰판.
운영 규칙 원본: `WIC_GLOBAL_OPERATING_RULES.md` 단일 원본만 사용.

---

# 1. 지금 한눈에 보기

- 제3자 외부검증 실제 연결: **0개 / HOLD**
- 제3자 외부검증 후보: **CircleCI / Codacy / BrowserStack**
- 현재 기능 PASS 도구: **확정 가능한 PASS 없음**
- 전체 완료율: **산정중(HOLD)** — 검증 완료 작업단위 전수 수치화 전에는 임의 % 금지
- 진행 원칙: **우선도구 실제 개선 시도 → 더 못 나가면 HOLD 기록 → 즉시 다음 실행 가능한 도구 → 새 근거가 생기면 우선도구로 복귀**

상태: 🟢 PASS = 실제 입력→실행→출력→검증 / 🟠 PARTIAL-HOLD = 일부 실기능 존재 / 🔴 HOLD = 현재 blocker / ⚪ NEXT = 다음 실행 대상

---

# 2. 이번 회차 실제 수행·새 발견

1. 최신 restart point를 먼저 읽고 `05/09 배포증거`, `10/14/19/22 index`, 저장소 목록 전수확인 등 완료 항목은 반복하지 않았다.
2. 중앙 허브에서 1번 JS 오류, 13번 업로드 endpoint/API, 6번 golden fixture/expected output을 직접 해제할 새 참조를 검색했으나 새 결과는 없었다. 이 검색은 다음 회차에서 그대로 반복하지 않는다.
3. **1번 실제 코드의 새 blocker를 특정했다.** `01-auto-guide-v1/index.html` 약 2500행대에서 Deno 결과의 `toc`가 비어 있으면 `defaultToc()`가 임의 시장 목차를 생성해 넣는 흐름을 확인했다. 이는 실제 자료 검증 없이 내용을 생성할 수 있으므로 현재 운영 원칙과 충돌한다.
4. 1번 코드에는 `HARD_DENO_URL = https://sweet-trout-31.obk369369-spec.deno.net`와 POST 호출 경로가 실제 존재한다. 다만 이번 환경에서 해당 endpoint의 현재 성공 응답 run/result 증거를 확보하지 못했으므로 PASS 금지.
5. 1번 대형 단일 HTML은 현재 연결의 전체파일 안전 수정/실행 검증이 부족하므로, 이번 회차에는 임의 TOC fallback을 직접 제거하지 않았다. **개선방법: 실제 실행 가능한 Work/브라우저 런타임에서 `if (!firstNormalized.toc) firstNormalized.toc = defaultToc(...)`를 제거하고 TOC 없음=HOLD로 전환 후 E2E 검증.**
6. 우선도구가 막혀 다음 실행 가능한 `05-report-generator`로 이동했다. 현재 전체 `index.html`을 읽어 실제 기능을 다시 확인했다. 현재 코드는 사용자 입력에 없는 시장동향·기업동향·전망 문구를 고정 본문으로 자동 생성하므로 사실기반 도구로는 위험하다.
7. 5번에 대해 **검증된 요약 필수 + 입력에 없는 사실 생성 금지** 형태의 수정안을 실제 파일교체로 적용하려 했으나 GitHub 쓰기가 적용되지 않아 commit이 생성되지 않았다. 따라서 코드가 바뀌었다고 기록하지 않으며 PARTIAL/HOLD 유지.
8. 외부검증 공식 경로를 최신 문서로 재확인했다. CircleCI는 조직 `VCS Connections → Install GitHub App → 저장소 선택`의 1회 관리자 설치 구조이며, Codacy는 GitHub로 로그인/권한 승인 후 저장소를 추가하면 최초 분석을 시작한다. 현재 사용자 승인 전이므로 실제 연결은 **0개** 그대로다.

---

# 3. 최우선 도구

| 우선 | 도구 | 상태 | 실제 확인된 것 | blocker | 개선방법 / 복귀조건 |
|---|---|---|---|---|---|
| 1 | 이메일 수집 | 🔴 HOLD | 공통 규칙·검증기준 | 전용 실행자산 미확인 | 새 실행파일/run URL 근거 발견 시 복귀 |
| 2 | 7번 고객 컨택 판단 | 🔴 HOLD | 잘못된 `07-wic-setting-tool-v1` 배제 | 실제 실행판 미확인 | 올바른 실행자산 근거 발견 시 복귀 |
| 3 | 1번 중간/최종 안내서 | 🔴 HOLD | 실제 HTML + Deno POST 코드 + **임의 TOC fallback 위험 특정** | endpoint 성공증거 없음, 대형파일 안전 수정/브라우저 E2E 미확보 | Work/브라우저 환경에서 synthetic TOC 제거→TOC 없음 HOLD→실제 입력 E2E |
| 4 | 37번 메타데이터 | 🔴 HOLD | 규칙 존재 | 생산 실행자산 미확인 | 실행자산 근거 발견 시 E2E |
| 5 | 13번 엑셀 자동 업로드 | 🟠 PARTIAL/HOLD | 입력→매핑→미리보기→XLSX 생성 | 실제 홈페이지 업로드 endpoint/API 없음 | endpoint/브라우저 자동화 근거 발견 시 복귀 |
| 6 | 6번 목차 정리 | 🟠 PARTIAL/HOLD | 저장소·엔진 흔적 | 승인 golden fixture/expected output 부족 | 승인 fixture 발견 시 회귀검증 |
| 7 | 2번 입찰 | 🔴 HOLD | 수동 UI 확인 | 나라장터 자동수집/API/로그인/투찰 엔진 없음 | 기존 실엔진 근거 발견 시 재사용 |
| 8 | 28~31 | 🔴 HOLD | 역할·규칙 분리 | 실행 저장소/파일 참조 없음 | 구체 실행자산 발견 시 복귀 |

---

# 4. 다음 실행 가능한 도구

| 도구 | 상태 | 이번 판단 | 다음 행동 |
|---|---|---|---|
| 05 Report Generator | 🟠 PARTIAL/HOLD | 현재 코드가 입력에 없는 일반 시장분석 문구를 생성. 안전 수정 시도는 commit 미생성 | 쓰기 가능한 경로 확보 시 최소 수정 후 실제 브라우저 실행검증; 막히면 09로 이동 |
| 09 Contents Making Tool | 🟠 PARTIAL/HOLD | index 존재, 실행 URL 없음 | 05 수정 불가 지속 시 실제 코드기능/수정 가능성 확인 |
| 08 English Verb Exercise | 🟠 PARTIAL/HOLD | Local-only | 업무 우선순위 낮음 |
| 27 Technical Book Verifier | 🟠 PARTIAL/HOLD | 과거 실행 UI만 확인 | 현재 실행자산 새 근거 있을 때 복귀 |
| 나머지 03/04/10/11/12/14/19/21/22/23/24/25/26 | 🔴 HOLD | 기존 확인 반복금지 | 새 파일명/run URL/기능근거가 생길 때만 재개 |

---

# 5. 외부검증 도입 상태 — 2026-08-13 기준 병행

| 외부구조 | 현재 | 실제 연결에 필요한 사용자 단계 | 이후 자동화 목표 |
|---|---|---|---|
| CircleCI | 🔴 0 | 조직 관리자 GitHub App 설치/저장소 승인 1회 | 외부 CI run/status를 WIC 증거로 수집 |
| Codacy | 🔴 0 | GitHub 로그인·권한 승인 후 저장소 추가 | 외부 정적분석/quality gate 결과 수집 |
| BrowserStack | 🔴 0 | 계정 credential 또는 승인 연결 | UI 도구 외부 브라우저 E2E |

GitHub Actions는 내부검증으로만 사용하며 제3자 독립검증으로 가장하지 않는다.

---

# 6. 이번 실제 개선점과 남은 핵심

**개선됨**
- 1번의 막연한 `JS 오류 미특정` 상태에서 한 단계 더 나아가 **실제 콘텐츠 조작 위험 코드(defaultToc synthetic fallback)**를 특정했다.
- 5번도 단순 UI 존재가 아니라 **입력하지 않은 분석문구를 생성하는 실제 기능 위험**을 특정했다.
- 외부검증은 후보 나열이 아니라 CircleCI/Codacy의 실제 사용자 승인 단계를 공식 문서 기준으로 좁혔다.

**남음**
- 1번 synthetic fallback 실제 삭제 commit + 실행검증
- 13번 실제 업로드 연결
- 6번 승인 fixture 확보
- 외부검증 서비스 최소 1개 실제 연결/run URL
- 이메일/7/37 실제 실행자산 식별

---

# 7. 막힘 처리 규칙

`원인 확인 → 가능한 최소 수정 1회 → 실행/검증 → 실패하면 HOLD + 개선방법 + restart point → 즉시 다음 도구`

같은 404/같은 저장소 검색/같은 HOLD 진단 반복 금지. 상태판 변경만 기능개선으로 계산하지 않는다.

---

# 8. 현재 restart point

- 중앙 허브의 `1 JS 오류 / 13 endpoint / 6 fixture` 일반 검색은 이번 회차 완료. 그대로 반복 금지.
- **1번:** 다음 실제 실행환경이 확보되면 synthetic TOC fallback 제거가 최우선 수정점. 그 전에는 동일 대형파일 조사 반복 금지.
- **5번:** 검증된 요약 필수/입력 밖 사실생성 금지 수정의 GitHub write가 실제 commit되지 않았으므로, 쓰기 가능한 경로에서 재시도할 가치가 있다. 같은 방식의 실패가 반복되면 즉시 09로 이동.
- **다음 즉시 실행 대상:** 05의 안전한 최소 수정 가능성을 한 번 더 다른 쓰기 경로로 확인하고, 불가하면 09 Contents Making Tool의 실제 코드 기능을 읽어 수정 가능한 위험/오류를 찾는다.
- **외부검증 병행:** CircleCI 또는 Codacy 중 사용자 1회 승인에 가장 가까운 경로를 확정하고, 승인 후 우선도구 저장소 1개로 최초 외부 run을 만드는 것을 목표로 한다.

이 파일은 새 파일을 만들지 않고 계속 같은 파일을 덮어쓴다.