# CUSTOMER GUIDE FORMAT MASTER

상태: ACTIVE / 유형별 안내서 양식 단일 기준
확정일: 2026-08-27
근거: 사용자 제공 최신 안내서 모음 + 2026-08-27 직접 교정

## 공통 핵심 규칙
1. 기존 안내서에 이미 붙인 추천자료를 사용자가 `개선`하라고 하면 자료 자체를 임의 교체하지 않는다. 먼저 동일 자료의 링크·목차·형식 오류만 수정한다. 자료 교체는 사용자가 교체를 지시하거나 해당 자료가 검증 FAIL일 때만 한다.
2. 현재 연도가 2026년인 국내보고서 작업은 2026년 발행자료를 우선이 아니라 기본 필수조건으로 탐색한다. 2026년 적합자료가 실제로 없을 때만 사용자 확인 또는 HOLD 후 과거 연도를 검토한다.
3. 고객용 상세링크는 실제 보고서 발행사 공식 상품 상세페이지를 최우선으로 사용한다. 리셀러·서점·중개사이트는 검색/교차검증용이며 고객용 최종 링크로 확정하지 않는다.
4. URL은 canonical/clean URL만 출력한다. utm_source=chatgpt.com, utm_* 및 검색·추적 파라미터를 제거한다.
5. 추천 2종 이상이면 가능한 한 서로 다른 실제 발행사를 우선한다. 단, 이미 사용자가 안내서에 붙인 자료를 개선하는 단계에서는 이 규칙 때문에 기존 자료를 임의 교체하지 않는다.
6. 국내보고서 목차는 실제 원문을 처음부터 끝까지 확인한다. 관련 부분만 발췌해 앞부분을 자르지 않는다.
7. 국내보고서 출력 목차 깊이는 `상위목차 + 바로 아래 하위목차` 2단계만 허용한다. 하하위목차 이하(1), (1), 가., A) 등)는 출력하지 않는다.
8. 하위목차는 한 단계 들여쓰기하고 번호를 복사 가능한 문자열로 보존한다.
9. 국내보고서 목차 완전성은 첫 상위목차 / 중간 상위목차 / 마지막 상위목차와 각 상위목차의 1단계 하위목차 존재를 원문과 대조한다.
10. 사용자가 `다시 뽑아`, `전체로 다시 뽑아`, `개선해서 다시 출력`이라고 하면 수정된 부분만 조각으로 내지 않는다. 같은 추천자료의 `제목 → 발행사/페이지/정가 → 발행일 → 공식 clean 링크 → 목차 전체(허용 깊이)`를 처음부터 끝까지 다시 출력한다.
11. 전체 재출력 전 제목·발행사·페이지·가격·발행일·공식 링크·목차를 다시 교차검증한다. 이전 출력값이 틀렸으면 조용히 그대로 계승하지 말고 수정된 값으로 전체본에 반영한다.
12. 검증이 덜 된 상태에서 `껍데기 목차`, 임의 요약 목차, 임시 링크를 전체본처럼 출력하지 않는다. 검증 실패 항목이 있으면 해당 항목만 HOLD로 명시하고 확정값처럼 쓰지 않는다.

## 국내 보고서 고정 출력 구조
1. 한글 자료명
2. `◇ 발행사: [국내 출판/조사기관]      ( [Pages] Pages)      ◇ 정가: \\[원화가격]`
3. `◇ 발행일: [YYYY] 년 [MM] 월 [DD] 일`
4. `자세한 내용의 링크 :`
5. 실제 발행사 공식 상품 상세페이지 clean URL
6. `목차`
7. 첫 상위목차부터 마지막 상위목차까지 상위+하위 2단계

출력 예:
`Ⅰ. ...`
`    1. ...`
`    2. ...`
`Ⅱ. ...`
`    1. ...`

## 국내 보고서 FAIL
- 사용자가 이미 붙인 자료를 `개선` 요청했는데 다른 자료로 임의 교체
- 사용자가 전체 재출력을 요구했는데 목차 등 일부 조각만 출력
- 2026년 적합자료가 있는데 2025년 이하 자료 추천
- URL에 ChatGPT/검색 추적 파라미터 잔존
- 리셀러 링크를 발행사 공식페이지처럼 확정
- 목차가 원문의 중간 관련 장부터 시작하여 위쪽이 잘림
- 상위 또는 바로 아래 하위목차 누락
- 하하위목차 이하 과다 출력
- 번호 소실 또는 들여쓰기 누락
- 존재하지 않는 목차 생성
- 페이지·가격·발행일 등 이전 오기값을 재검증 없이 반복
- 검증되지 않은 임시 링크/요약 목차를 완성본처럼 출력

## 전민석 사례 체크포인트 (2026-08-27)
이미 안내서에 붙인 3종을 유지하고 목차/링크를 우선 개선한다.
- 추천1: `2026년 국내 주력산업(반도체, 디스플레이, 이차전지) 관련 기술, 시장 전망과 유망 전략품목 사업화 전략` / 씨에치오 얼라이언스 / 2026-04-30 / 621p / 정가 480,000원
- 추천2: `2026년 중소·중견기업형 유망기술 연구개발 테마 총람(Ⅱ) - 반도체, 디스플레이, 배터리/소재, 부품, 장비산업 -` / 씨에치오 얼라이언스 / 2026-08-20 / 페이지 수는 재검증 필요 / 정가 400,000원
- 추천3: `첨단산업 핵심 공정기술 동향분석-디스플레이·이차전지·AI 반도체` / 지식산업정보원 / 2026-06-15 / 626p / 정가 420,000원
세 자료 모두 2026년 자료이므로 연도 조건 PASS. 동일 발행사 회피는 향후 신규 선정에 적용하되 이미 붙인 추천1·2를 개선 단계에서 임의 교체하지 않는다.

## 기타 유형
영문 시장조사보고서, 영문 공학도서, 일본어 서적/리포트는 기존 유형별 최신 수동 완성본과 기존 LOCK 규칙을 계속 적용한다. 유형을 섞지 않으며 원발행사 링크, 실제 목차, 번호 보존 원칙을 유지한다.
