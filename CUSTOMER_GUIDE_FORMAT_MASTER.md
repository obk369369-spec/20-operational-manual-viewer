# CUSTOMER GUIDE FORMAT MASTER

상태: ACTIVE / 유형별 안내서 양식 단일 기준
확정일: 2026-08-27
근거: 사용자 제공 최신 안내서 모음 + 2026-08-27 직접 교정

## 공통 핵심 규칙
1. 기존 안내서에 이미 붙인 추천자료를 사용자가 `개선`하라고 하면 자료 자체를 임의 교체하지 않는다. 먼저 동일 자료의 링크·목차·형식 오류만 수정한다. 자료 교체는 사용자가 교체를 지시하거나 해당 자료가 검증 FAIL일 때만 한다.
2. 현재 연도가 2026년인 국내보고서 작업은 2026년 발행자료를 기본 필수조건으로 탐색한다. 2026년 적합자료가 실제로 없을 때만 HOLD 후 과거 연도를 검토한다.
3. 국내보고서의 최우선 원칙은 `1개 보고서 = 1개 확정 상세페이지 = 그 페이지의 데이터만 사용`이다. 원발행사 공식 상품페이지를 먼저 찾되, 찾지 못하면 자료명과 실제 발행사가 정확히 일치하는 신뢰 가능한 판매/유통/서점 상세페이지 하나를 확정한다.
4. 한 보고서의 제목·발행사·페이지·가격·발행일·목차를 여러 사이트에서 섞지 않는다. 다른 사이트는 후보 탐색 또는 오류 확인용으로만 볼 수 있으며, 최종 출력 데이터는 확정한 한 페이지에서만 가져온다.
5. 확정 상세페이지에서 일부 필드가 없으면 다른 사이트 값으로 몰래 보충하지 않는다. 없는 필드는 HOLD 또는 미표시로 두고, 필요하면 해당 보고서를 다른 후보로 교체한다.
6. URL은 canonical/clean URL만 출력한다. `utm_source=chatgpt.com`, `utm_*` 및 검색·추적 파라미터를 제거한다.
7. 추천 2종 이상이면 가능한 한 서로 다른 실제 발행사를 우선한다. 단, 이미 사용자가 안내서에 붙인 자료를 개선하는 단계에서는 이 규칙 때문에 기존 자료를 임의 교체하지 않는다.
8. 국내보고서 목차는 확정 상세페이지에 보이는 목차를 처음부터 끝까지 그대로 따라간다. 관련 부분만 발췌하거나 다른 사이트의 목차를 섞지 않는다.
9. 국내보고서 출력 목차 깊이는 `상위목차 + 하위목차 + 하하위목차` 3단계까지 출력한다. 그보다 아래 단계는 제외한다.
10. 하위목차는 한 단계, 하하위목차는 두 단계 들여쓰기하고 번호를 복사 가능한 문자열로 보존한다.
11. 국내보고서 목차 완전성은 확정 상세페이지 기준으로 첫 상위목차 / 중간 상위목차 / 마지막 상위목차와 각 상위목차의 하위·하하위목차 존재를 확인한다.
12. 사용자가 `다시 뽑아`, `전체로 다시 뽑아`, `개선해서 다시 출력`이라고 하면 수정된 부분만 조각으로 내지 않는다. 같은 추천자료의 `제목 → 발행사/페이지/정가 → 발행일 → clean 링크 → 목차 전체(허용 깊이)`를 처음부터 끝까지 다시 출력한다.
13. 전체 재출력 전에는 여러 사이트를 섞어 교차조립하지 말고, 확정 상세페이지와 최종 출력값을 1:1 대조한다. 제목과 발행사가 맞고 그 페이지에서 실제 확인되는 데이터만 붙인다.
14. 검증이 덜 된 상태에서 껍데기 목차, 임의 요약 목차, 다른 사이트에서 가져온 목차, 임시 값을 완성본처럼 출력하지 않는다.
15. 사용자가 결과를 직접 빠르게 확인할 수 있도록 각 추천자료에 `데이터·목차 출처`를 표시한다. 이 출처는 바로 위 `자세한 내용의 링크`와 동일한 확정 상세페이지여야 한다. 별도 출처가 섞이면 FAIL이다.
16. 사용자가 별도로 `업데이트`라고 말하지 않아도, 실제 작업 중 새 규칙·교정·재발 오류가 확정되면 해당 중앙마스터에 즉시 증분 반영하고 commit 후 remote read-back까지 수행한다. 같은 규칙이면 중복 작성하지 않고 recurrence/교정으로 통합한다.

## 국내 보고서 고정 출력 구조
1. 한글 자료명
2. `◇ 발행사: [국내 출판/조사기관]      ( [Pages] Pages)      ◇ 정가: \\[원화가격]`
3. `◇ 발행일: [YYYY] 년 [MM] 월 [DD] 일`
4. `자세한 내용의 링크 :`
5. 확정 상세페이지 clean URL
6. `데이터·목차 출처: 위 상세페이지와 동일`
7. `목차`
8. 첫 상위목차부터 마지막 상위목차까지 상위+하위+하하위 3단계

출력 예:
`Ⅰ. ...`
`    1. ...`
`        1-1. ...`
`        1-2. ...`
`    2. ...`
`Ⅱ. ...`
`    1. ...`
`        1-1. ...`

## 국내 보고서 FAIL
- 한 보고서의 제목/발행사/페이지/가격/발행일/목차를 서로 다른 사이트에서 혼합
- 자세한 내용의 링크와 목차 출처가 서로 다름
- 타이틀·발행사가 일치하지 않는 페이지의 데이터 사용
- 사용자가 이미 붙인 자료를 `개선` 요청했는데 다른 자료로 임의 교체
- 사용자가 전체 재출력을 요구했는데 목차 등 일부 조각만 출력
- 2026년 적합자료가 있는데 2025년 이하 자료 추천
- URL에 ChatGPT/검색 추적 파라미터 잔존
- 공식 링크를 못 찾았다는 이유로 확인 가능한 대체 상세페이지조차 사용하지 않음
- 목차가 확정 페이지의 원문 중간부터 시작하여 위쪽이 잘림
- 상위·하위·하하위목차가 확정 페이지 원문 대비 누락
- 허용 깊이보다 더 아래 단계까지 과다 출력
- 번호 소실 또는 들여쓰기 누락
- 존재하지 않는 목차 생성 또는 모델 요약 목차 사용
- 페이지·가격·발행일 등 이전 오기값을 확정 페이지 재확인 없이 반복
- 확정된 사용자 피드백을 중앙마스터에 반영하지 않고 대화에만 남김

## 전민석 사례 체크포인트 (2026-08-27)
이미 안내서에 붙인 3종을 유지하고 링크/목차를 개선한다.
- 추천1: `2026년 국내 주력산업(반도체, 디스플레이, 이차전지) 관련 기술, 시장 전망과 유망 전략품목 사업화 전략` / 씨에치오 얼라이언스 / 2026-04-30 / 정가 480,000원
- 추천2: `2026년 중소·중견기업형 유망기술 연구개발 테마 총람(Ⅱ) - 반도체, 디스플레이, 배터리/소재, 부품, 장비산업 -` / 씨에치오 얼라이언스 / 2026-08-20 / 정가 400,000원
- 추천3: `첨단산업 핵심 공정기술 동향분석-디스플레이·이차전지·AI 반도체` / 지식산업정보원 / 2026-06-15 / 정가 420,000원
세 자료 모두 2026년 자료이므로 연도 조건 PASS. 동일 발행사 회피는 향후 신규 선정에 적용하되 이미 붙인 추천1·2를 개선 단계에서 임의 교체하지 않는다.
추천2와 추천3은 이전 출력에서 목차를 다른 사이트/요약값과 혼합한 재발 오류가 확인되었으므로 기존 목차 출력은 무효 처리한다. 추천3 원문에는 `제 2장 차세대 배터리 개발과 열폭주 제어/냉각 성능연구`가 존재한다는 사용자 확인을 회귀 기준으로 보존하며, 재출력 때는 확정 상세페이지 원문과 직접 대조한다.

## 기타 유형
영문 시장조사보고서, 영문 공학도서, 일본어 서적/리포트는 기존 유형별 최신 수동 완성본과 기존 LOCK 규칙을 계속 적용한다. 유형을 섞지 않으며 원발행사 링크, 실제 목차, 번호 보존 원칙을 유지한다.
