# CUSTOMER GUIDE FORMAT MASTER

상태: ACTIVE / 유형별 안내서 양식 단일 기준
확정일: 2026-08-27
근거: 사용자 제공 최신 안내서 모음 + 2026-08-27 직접 교정

## 공통 핵심 규칙
1. 기존 안내서에 이미 붙인 추천자료를 사용자가 `개선`하라고 하면 자료 자체를 임의 교체하지 않는다. 먼저 동일 자료의 링크·목차·형식 오류만 수정한다. 자료 교체는 사용자가 교체를 지시하거나 해당 자료가 검증 FAIL일 때만 한다.
2. 현재 연도가 2026년인 국내보고서 작업은 2026년 발행자료를 기본 필수조건으로 탐색한다. 2026년 적합자료가 실제로 없을 때만 HOLD 후 과거 연도를 검토한다.
3. 국내보고서는 검색 순서에서 `읽기에 성공한 첫 상세페이지` 중 타이틀 + 발행사 + 발행일이 모두 정확히 일치하면 그 페이지를 즉시 확정한다.
4. 먼저 발견한 사이트가 읽기 실패, 페이지 없음, 또는 타이틀/발행사/발행일 불일치이면 그 사이트는 버리고 다음 사이트를 찾는다. 다음 사이트가 읽기에 성공하고 타이틀 + 발행사 + 발행일이 모두 일치하면 그 사이트가 새로운 `최초 확정 사이트`가 된다.
5. 최초 확정 사이트가 정해진 뒤에는 다른 사이트를 더 찾지 않는다. 교차검증, 보충, 보완, 대조, 재검색을 하지 않는다.
6. 제목·발행사·페이지·가격·발행일·링크·목차 등 최종 출력 데이터는 최초 확정 사이트 한 페이지에서 보이는 값만 사용한다.
7. 확정 상세페이지에 없는 값은 다른 사이트에서 가져오지 않는다. 임의 생성·추정·보충도 하지 않는다. 해당 값은 미표시 또는 HOLD 처리한다.
8. URL은 확정 상세페이지의 clean URL만 출력한다. `utm_source=chatgpt.com`, `utm_*` 및 검색·추적 파라미터만 제거한다.
9. 추천 2종 이상이면 신규 선정 단계에서 가능한 한 서로 다른 실제 발행사를 우선한다. 다만 이미 안내서에 붙인 자료 개선 단계에서는 기존 자료를 임의 교체하지 않는다.
10. 국내보고서 목차는 확정한 첫 상세페이지에 보이는 목차만 사용한다. 다른 사이트의 목차를 섞거나 모델이 요약·재구성하지 않는다.
11. 국내보고서 출력 목차 깊이는 `상위목차 + 하위목차 + 하하위목차` 3단계까지 출력하고 그보다 아래 단계는 제외한다.
12. 하위목차는 한 단계, 하하위목차는 두 단계 들여쓰기하고 번호를 복사 가능한 문자열로 보존한다.
13. 여러 추천자료를 한 번에 출력할 때는 발행일 기준 내림차순으로 정렬한다. 가장 최신 발행일 자료를 추천 1/맨 위에 두고, 아래로 갈수록 발행일이 오래되도록 배치한다. 동일 발행일이면 고객 적합도가 높은 자료를 먼저 둔다.
14. 사용자가 `다시 뽑아`, `전체로 다시 뽑아`, `개선해서 다시 출력`이라고 하면 같은 확정 상세페이지를 그대로 재사용해 `제목 → 발행사/페이지/정가 → 발행일 → clean 링크 → 목차`를 전체 재출력한다.
15. 사용자가 결과를 직접 확인할 수 있도록 각 추천자료에 `데이터·목차 출처: 위 상세페이지와 동일`을 표시한다. 이 문구는 실제로 모든 출력값이 그 한 페이지에서만 왔을 때만 사용한다.
16. 사용자가 별도로 `업데이트`라고 말하지 않아도, 실제 작업 중 새 규칙·교정·재발 오류가 확정되면 해당 중앙마스터에 즉시 증분 반영하고 commit 후 remote read-back까지 수행한다. 같은 규칙이면 중복 작성하지 않고 recurrence/교정으로 통합한다.
17. 고객별 추천자료를 실제로 확정하면 `고객/자료명/최초 확정 사이트 clean URL/발행사/발행일/상태(PASS·HOLD)`를 후속 대화창에서도 조회할 수 있는 중앙 기록에 남긴다. 대화창이 바뀌어도 사용자가 `이 자료를 어느 사이트에서 찾았지?`라고 물으면 해당 기록을 먼저 조회해 답한다. 대화에만 남기고 중앙 기록에 저장하지 않은 URL은 후속 대화에서 기억된다고 단정하지 않는다.
18. 운영 피드백·확정 출처·고객별 작업상태·재발 오류는 대화창 수명에 의존하지 않는다. 대화창이 삭제·변경·이동되더라도 후속 작업에서 복원할 수 있도록, 확정되는 즉시 중앙마스터 또는 중앙 기록에 영구 누적한다. `현재 대화에만 기록`된 상태를 완료로 보지 않는다.
19. Work 투입은 이 중앙 누적을 자동화·검증하는 후속 단계로 사용한다. Work 미투입 상태에서도 현재 채팅 작업에서 확정된 피드백은 즉시 중앙마스터/중앙 기록에 반영하며, Work가 나중에 이 기록을 TOOL001의 자동 검증·출력 모듈로 승격하도록 한다.

## 고객 컨택 멘트·선택 분기 카드 고정 규칙
20. 고객에게 전화할 멘트를 작성할 때는 `실제 읽는 전화 멘트`와 별도로 `통화용 큐 카드`를 반드시 함께 출력한다. 큐 카드의 목적은 멘트를 다시 써주는 것이 아니라, 사용자가 통화 중 문장을 계속 읽다가 말이 꼬이지 않도록 다음에 할 말을 한눈에 떠올리게 하는 것이다.
21. 큐 카드 안에는 전화 멘트의 문장·설명문을 복사하지 않는다. 카드 1개에는 `고객 업무 핵심`, `자료 방향 핵심`, `내가 물을 핵심` 등 한 단계의 뜻만 매우 짧은 단어·짧은 구로 표시한다. 원칙적으로 한 카드가 한눈에 즉시 읽혀야 하며 긴 문장·여러 줄 설명은 FAIL이다.
22. 큐 카드는 통화 흐름 순서대로 배치하고 카드 사이에는 `↓` 또는 `→` 화살표를 넣어 다음 말의 순서를 눈으로 바로 따라갈 수 있게 한다. 카드만 훑어도 `누구/무슨 일 → 어떤 자료 방향 → 무엇을 물을지`가 이어져야 한다.
23. `전화 멘트`는 실제 입으로 자연스럽게 말할 완성 문장이고, `큐 카드`는 그 멘트를 외우거나 그대로 읽는 축약본이 아니다. 둘의 문구가 거의 같으면 카드 기능 FAIL로 본다.
24. 고객의 연구/업무 분야가 둘 이상으로 나뉠 수 있으면 단일 질문으로 끝내지 않고 2~4개의 명확한 `방향`으로 분기한다. 고객이 전화에서 어느 쪽인지 쉽게 고를 수 있게 질문형으로 제시한다.
25. 방향 분기는 모델이 연구분야만 보고 임의 생성하면 안 된다. 멘트를 만들기 전에 온라인에서 각 방향에 실제 판매·안내 가능한 보고서/도서/자료 타이틀이 존재하는지 먼저 검색한다. 최소 1개의 실제 타이틀이 확인된 방향만 고객 선택지로 올린다.
26. 각 분기에는 내부 확인용으로 `방향명 / 실제 확인한 자료 타이틀 / 출처 사이트 / 확인상태`를 연결해 둔다. 고객에게 전화할 때는 타이틀을 전부 읽지 않아도 되지만, 사용자가 나중에 `이 방향에 실제 자료가 있었나?`라고 물으면 즉시 근거를 제시할 수 있어야 한다.
27. 실제 자료 타이틀을 못 찾은 방향은 고객에게 선택지로 먼저 제시하지 않는다. 연구상 가능성이 있어 보여도 판매 가능한 자료 존재 여부가 미확인이면 `HOLD`로 두고, 확인 후에만 분기에 추가한다.
28. 고객 컨택 출력은 `① 실제 읽는 전화 멘트 → ② 통화용 큐 카드(초단축·화살표 흐름) → ③ 내부용 실제 자료 타이틀 근거`를 기본으로 한다. 필요할 때만 고객 답변별 다음 멘트를 추가한다.
29. 고객의 과거 문의·구매·답장·통화 내용이 있으면 분기보다 먼저 반영한다. 단순 회사소개 메일 발송만 있었던 경우 이를 고객의 관심 확정으로 취급하지 않는다.
30. 새 고객 멘트 작업에서도 온라인 사전조사 결과와 선택 분기를 중앙 기록에 남겨 후속 대화창에서 재사용한다.
31. 분기 순서는 반드시 `고객의 실제 담당업무 확인 → 그 업무와 직접 연결되는 실제 판매자료를 먼저 검색 → 확인된 자료들의 핵심 내용과 반복되는 흐름을 묶음 → 그 묶음을 유치원생도 바로 이해할 생활말로 변환 → 2~4개 선택지 작성 → 최종 전화 멘트 작성 → 멘트와 별개의 초단축 큐 카드 작성`으로 고정한다.
32. `분기 아이디어를 먼저 만든 뒤 그 분기에 맞는 보고서를 나중에 찾는 방식`은 금지한다. 실제 자료 조사보다 먼저 만들어진 선택지는 근거 없는 임의 분기로 보고 폐기한다.
33. 전화에서 말하는 분기 문구는 보고서 제목이나 전문용어를 그대로 줄여 쓰는 것이 아니라, 실제 확인한 보고서 내용을 유치원생도 장면을 떠올릴 수 있는 생활말로 풀어쓴다. 단, 쉬운 표현 뒤에는 내부 근거로 실제 자료 타이틀과 출처를 유지한다.
34. 큐 카드의 쉬운 표현은 `유치원생에게 설명하는 장문`이 아니라 `사용자 본인이 통화 중 1초 안에 뜻을 떠올리는 표지판`이어야 한다. 쉬우면서도 뜻이 모호하면 안 된다. 카드만 보고 무슨 뜻인지 다시 해석해야 하는 표현은 FAIL이다.

## 고객 컨택 멘트 FAIL
- 통화용 큐 카드 없이 전화 멘트 본문만 출력
- 큐 카드에 전화 멘트와 같은 문장을 반복함
- 큐 카드 안에 긴 설명문·여러 문장을 넣어 통화 중 한눈에 읽히지 않음
- 카드 사이 화살표/흐름 표시가 없어 다음 순서를 즉시 파악하기 어려움
- 카드가 너무 축약돼 사용자가 다시 뜻을 해석해야 함
- 고객이 선택할 수 있는 방향 분기 없이 `필요한 자료가 있습니까?` 같은 포괄 질문만 함
- 온라인 사전조사 없이 연구분야만 보고 방향을 임의 생성
- 분기 아이디어를 먼저 만든 뒤 그 방향에 맞는 보고서를 사후 검색함
- 실제 보고서/도서 타이틀이 확인되지 않은 방향을 선택지로 제시
- 실제 보고서 내용은 확인했지만 전문용어를 그대로 분기 문구에 사용하여 쉽게 풀어쓰지 않음
- 방향별 실제 자료 근거를 저장하지 않아 나중에 출처·타이틀을 복원하지 못함
- 과거 고객 반응 없이 단순 발송 이력만 관심 확정으로 오판

## 국내 보고서 고정 출력 구조
1. 한글 자료명
2. `◇ 발행사: [국내 출판/조사기관]      ( [Pages] Pages)      ◇ 정가: \\[원화가격]`
3. `◇ 발행일: [YYYY] 년 [MM] 월 [DD] 일`
4. `자세한 내용의 링크 :`
5. 최초 확정 상세페이지 clean URL
6. `데이터·목차 출처: 위 상세페이지와 동일`
7. `목차`
8. 확정 상세페이지에 표시된 목차 중 상위+하위+하하위 3단계

## 국내 보고서 FAIL
- 읽기 실패한 최초 발견 사이트를 계속 붙잡고 작업함
- 타이틀 + 발행사 + 발행일이 일치하는 읽기 가능한 페이지를 찾았는데 다른 사이트를 계속 찾음
- 다른 사이트에서 페이지·가격·발행일·목차를 보충 또는 보완함
- 한 보고서의 데이터를 여러 사이트에서 혼합함
- 자세한 내용의 링크와 데이터·목차 출처가 다름
- 타이틀·발행사·발행일이 일치하지 않는 페이지의 데이터 사용
- 확정 페이지에 없는 값을 추정·생성·보충함
- 사용자가 이미 붙인 자료를 `개선` 요청했는데 다른 자료로 임의 교체
- 사용자가 전체 재출력을 요구했는데 목차 등 일부 조각만 출력
- 2026년 적합자료가 있는데 2025년 이하 자료 추천
- 여러 추천자료를 최신 발행일 순으로 정렬하지 않음
- URL에 ChatGPT/검색 추적 파라미터 잔존
- 확정 페이지의 목차가 아닌 다른 사이트 목차 또는 모델 요약 목차 사용
- 허용 깊이보다 더 아래 단계까지 과다 출력
- 번호 소실 또는 들여쓰기 누락
- 확정된 사용자 피드백을 중앙마스터에 반영하지 않고 대화에만 남김
- 확정한 고객별 최초 사이트 URL을 중앙 기록에 남기지 않아 후속 대화에서 출처를 복원하지 못함
- 대화창 삭제·이동 후 복원할 운영 피드백/출처/상태를 중앙 기록 없이 대화에만 의존함

## 전민석 사례 체크포인트 (2026-08-27)
이미 안내서에 붙인 3종을 유지하고 링크/목차를 개선한다.
- 추천1: `2026년 국내 주력산업(반도체, 디스플레이, 이차전지) 관련 기술, 시장 전망과 유망 전략품목 사업화 전략` / 씨에치오 얼라이언스 / 2026-04-30 / 정가 480,000원
- 추천2: `2026년 중소·중견기업형 유망기술 연구개발 테마 총람(Ⅱ) - 반도체, 디스플레이, 배터리/소재, 부품, 장비산업 -` / 씨에치오 얼라이언스 / 2026-08-20 / 정가 400,000원
- 추천3: `첨단산업 핵심 공정기술 동향분석-디스플레이·이차전지·AI 반도체` / 지식산업정보원 / 2026-06-15 / 정가 420,000원
세 자료 모두 2026년 자료이므로 연도 조건 PASS. 추천2와 추천3의 과거 혼합 목차 출력은 무효 처리하고, 다음 재출력부터는 각 자료별로 최초 확정한 단일 상세페이지의 데이터만 사용한다.
전민석 3종을 새로 정렬하면 발행일 내림차순은 `2026-08-20 → 2026-06-15 → 2026-04-30` 순서다.

## 기타 유형
영문 시장조사보고서, 영문 공학도서, 일본어 서적/리포트는 기존 유형별 최신 수동 완성본과 기존 LOCK 규칙을 계속 적용한다. 유형을 섞지 않으며 원발행사 링크, 실제 목차, 번호 보존 원칙을 유지한다.