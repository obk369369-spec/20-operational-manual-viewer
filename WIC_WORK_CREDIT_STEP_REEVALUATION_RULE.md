# WIC Work 크레딧 초압축 단계운영 규칙

적용일: 2026-09-16

이 규칙은 WIC Work의 공통 운영규칙으로 사용한다.

## 핵심 실행식

`전체 잔여 작업 확인 → PASS/중복/무효/변화 없는 HOLD 제거 → 같은 원인의 실제 미완료를 큰 묶음으로 초압축 → 실제 수정/시험/배포 → 전체 잔여 작업으로 복귀 → 다음 큰 묶음 선택 → 지정 재평가선에서 전체 재평가`

## 강제 규칙

1. 한 묶음 완료는 WIC 전체 완료가 아니다.
2. 한 작업이 HOLD되어도 다른 독립 작업을 중단하지 않는다.
3. 기존 PASS, 완료된 추출, 무효화된 오류, 변화 없는 플랫폼 HOLD는 재조사·재시험하지 않는다.
4. 오류 1개마다 수정/시험/commit/push/read-back을 반복하지 않는다. 가능한 변경과 시험은 큰 묶음으로 압축한다.
5. 각 큰 묶음이 끝나면 특정 TOOL에 머무르지 말고 WIC 전체 잔여판으로 돌아가 다음 묶음을 선택한다.
6. 재평가선에 가까워지면 새 큰 묶음을 시작하지 않는다. 진행 중 묶음을 안전하게 닫기 어렵다면 더 일찍 멈춘다.
7. 재평가선까지 크레딧을 억지로 소비하지 않는다. 가치 있는 준비 작업이 먼저 소진되면 남긴다.
8. 재평가 시 `완료 / 실제 미완료 / 새 오류 / 외부자료 대기 / 플랫폼 HOLD / 다음 큰 묶음 / SAFE_CHECKPOINT`를 WIC 전체 기준으로 다시 산출한다.
9. 사용자에게 오류 전달, 대화창 간 복사, 파일 재탐색, 결과 비교, GitHub/read-back 확인, HOLD 회수, 반복 테스트를 맡기지 않는다. 자동화 가능한 일을 사용자가 해야 하면 `OBSERVER_MODE=FAIL`이다.
10. 실제 코드·실행·시험·배포 증거가 없는 상태표/문서만으로 PASS하지 않는다.

## 현재 단계 운영

현재 다음 재평가선은 `40%`다.

현재 확정된 우선 실행 묶음은 기존 `Issue → TOOL016 → TOOL044 worker → TOOL016 결과귀환` PASS 경로를 재개발하지 않고 다음 잔여를 한 묶음으로 닫는 것이다.

`조기 PASS 차단 → 독립 영수증 교차검증 → 영수증 불일치 차단 → 중앙 상태 쓰기 실패복구 → 중단 후 재시작 연속성 → 결과귀환 → 최소 GitHub 반영/read-back → 실제 사용본 배포/재시험`

확인된 변경 후보 지점은 `feedback_pipeline/tool044_atomic_watch_v2.py`의 worker 결과 귀환 처리 부분이다. 독립 영수증 대조 전에 ACK/PASS를 확정하지 않아야 한다.

이 묶음이 끝나면 즉시 WIC 전체 잔여 작업으로 복귀하여 TOOL006/013/002/043 및 공통 자동순환/관찰자 구조 등 다음 실제 완주 가능한 큰 묶음을 선택한다.

`40% 도달 = WIC 전체 완료`가 아니다. 40%에서 전체를 다시 펼쳐 다음 재평가선과 작업 묶음을 새로 결정한다.
