# WIC 대화창 소유권·이름 잠금 규칙

상태: NON-NORMATIVE COMPATIBILITY REFERENCE / EXECUTION SOURCE 아님
규범 원본: `WIC_GLOBAL_OPERATING_RULES.md`
적용 범위: 과거 대화창 생성·이름 잠금 규칙의 호환 참조

> 실행 우선순위: 이 파일의 상세 문구는 독립 규범 원본으로 사용하지 않는다. 현재 실행 규칙은 반드시 `WIC_GLOBAL_OPERATING_RULES.md`를 먼저 적용한다. 이 파일과 Global이 충돌하면 Global이 우선하며, 새 변경사항은 이 파일에 추가하지 않고 Global에만 통합한다.

## 1. 대화창 생성 권한
- 대화창 생성은 사용자(관찰자)만 수행한다.
- Assistant/Agent/도구는 사용자의 명시적 지시 없이 새 대화창을 만들지 않는다.
- 사용자가 기존 대화창에서 작업을 지시하면 반드시 그 대화창에서 이어서 작업한다.
- 작업 편의를 이유로 새 대화창을 만들거나 다른 대화창으로 우회하지 않는다.
- 기본 실행 원칙은 `CREATE`가 아니라 `REUSE`다. 새 구조가 필요해 보여도 먼저 기존 대화창·기존 작업·기존 규칙의 재사용 가능성을 확인한다.

## 2. 생성 권한으로 확대 해석하면 안 되는 지시
- `보고해라`, `계속해라`, `재개해라`, `확인해라`, `검증해라`, `진행 상황을 알려줘`와 같은 상태·계속·검증 지시는 기존 작업을 이어서 수행하라는 뜻이며 새 대화창 생성 권한이 아니다.
- 새 작업, 새 역할, 새 이름, 새 대화창이 편리하다는 판단만으로 사용자 허가를 추정하지 않는다.
- 사용자가 예약·자동화를 허가한 경우에도 그 허가는 자동으로 새 대화창 생성·명명·변경 권한까지 포함하지 않는다.

## 3. 예약·자동화 사전 차단
- 예약·자동화 생성 전에 기존 자동화 재사용 가능 여부와 대화창 생성 부작용 가능성을 먼저 확인한다.
- 제품 구조상 예약·자동화가 연결 대화 또는 별도 대화 표면을 만들 가능성이 있고 사용자가 새 대화창을 금지했다면, 그 예약·자동화를 새로 만드는 방식으로 문제를 해결하지 않는다.
- 이미 존재하는 예약·자동화의 실행은 그 자체로 새로운 대화창 생성 허가로 확대하지 않는다.
- 오류를 고치기 위해 또 다른 감시·복구·보고 예약을 연쇄적으로 추가하지 않는다. 동일 목적이면 기존 대표 작업에 통합한다.

## 4. 대화창 이름 변경 금지
- 대화창 이름은 사용자(관찰자)만 변경할 수 있다.
- Assistant/Agent/도구는 사용자의 명시적 지시 없이 대화창 제목을 변경하거나 다른 이름으로 취급하지 않는다.
- 현재 대화창의 사용자가 지정한 이름을 그대로 유지한다.
- 이름 변경 필요성이 있어도 먼저 사용자에게 보고하고, 사용자가 직접 변경하기 전까지 기존 이름을 유지한다.

## 5. 현재 대화창 식별 규칙 — 특정 창 이름 전역 고정 금지
- 이 전역 잠금 문서에는 특정 대화창 이름을 `현재 대화창`으로 하드코딩하지 않는다.
- 현재 대화창의 실제 UI 제목은 현재 실행 컨텍스트 또는 접근 가능한 직접 증거에서 확인된 값만 사용한다.
- 논리 역할명, 과거 registry 별칭, 자동화 제목, task 제목, restart label을 실제 UI 대화창 제목으로 간주하지 않는다.
- 직접 증거가 없으면 `UI_TITLE_HOLD`로 처리하며 임의 이름을 생성·추정·변경하지 않는다.
- 특정 대화창에서 받은 지시는 사용자가 명시적으로 다른 기존 대화창으로 이동하라고 하지 않는 한 그 대화창의 연속 작업으로 처리한다.

## 6. 중복 작업·규칙 통합
- 비슷한 컴플레인·지적·오류가 다시 들어와도 새 규칙을 덧붙이기 전에 대표 규칙과 의미를 대조한다.
- 의미가 같으면 새 규칙·새 자동화·새 감시 작업을 만들지 않고 기존 대표 규칙을 그대로 사용한다.
- 실제로 새로운 요구만 기존 대표 규칙에 최소 병합한다.
- 과거 중복 작업은 `완전 중복 / 부분 중복 / 별도 유지`로 구분하고, 활성 정상 업무를 임의로 삭제·비활성화하지 않는다.

## 7. 통합 작업 시 예외 없음
- 고객 안내 대화창 통합, 이메일 수집 대화창 통합, 중앙 규칙 통합을 진행하더라도 기존 대화창 제목을 임의로 변경하지 않는다.
- 통합은 GitHub 중앙 규칙/데이터/라우팅 구조를 합치는 작업이며, 대화창 이름을 자동 변경하거나 새 창을 자동 생성하는 작업이 아니다.
- 통합 후에도 실제 대화창 생성·이름 변경 권한은 사용자에게만 있다.

## 8. 위반 시 판정
- 무단 새 대화창 생성: FAIL
- 무단 대화창 이름 변경 또는 다른 이름으로 취급: FAIL
- 상태·보고·재개 지시를 새 대화창 생성 권한으로 확대 해석: FAIL
- 새 대화창 금지 조건에서 대화창 생성 부작용이 가능한 새 예약·자동화를 우회책으로 추가: FAIL
- 특정 대화창 이름을 전역 규칙에 하드코딩하여 다른 대화창에 오적용: FAIL
- 논리 역할명·자동화명·별칭을 실제 UI 대화창 제목으로 단정: FAIL
- 기존 창에서 이어서 하라는 지시를 무시하고 새 창으로 작업: FAIL
- 동일 목적의 감시·복구·보고 규칙을 중복 생성: FAIL
- 위반 발견 시 추가 변경을 중단하고 현재 실행 컨텍스트의 직접 증거로 식별 가능한 기존 대화창을 재개점으로 사용한다.

## 9. PASS 기준
- 상태·보고·재개·검증 지시 반복 시 신규 대화창 생성 0건
- 무허가 대화창 이름 생성·변경 0건
- 동일 의미 지적 반복 시 신규 중복 규칙·중복 감시 작업 생성 0건
- 기존 정상 대화창·업무·자동화의 범위와 우선순위에 영향 0건
- 직접 증거가 없는 UI 제목은 추정하지 않고 `UI_TITLE_HOLD` 유지
- 위 조건을 실제 확인하기 전에는 최종 PASS로 표시하지 않는다.

## 10. 핵심 한 줄
관찰자만 대화창을 만들고 이름을 바꾼다. Assistant/Agent는 생성보다 기존 구조 재사용을 우선하고, 상태 지시나 자동화를 새 대화창 권한으로 확대하지 않으며, 직접 검증된 현재 대화창에서만 이어서 작업한다.
