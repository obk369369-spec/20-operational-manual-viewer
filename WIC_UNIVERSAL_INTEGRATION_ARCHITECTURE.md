# WIC UNIVERSAL INTEGRATION ARCHITECTURE — 완전 범용 통합 구조

상태: ACTIVE / LOCK
목적: 현재 도구와 앞으로 추가될 모든 도구·대화창·업무가 동일한 구조를 재사용하게 하여 규칙 중복, 재설명, 재개발, 충돌검사 지연을 줄이고 통합 속도를 높인다.

## 1. 최상위 구조
모든 WIC 업무는 아래 4단계를 따른다.

`공통마스터 → 업무군 규칙 → 분야/도구 예외 → 실제 데이터`

### 1) 공통마스터
- 전 업무 공통 운영원칙
- 관찰자 모드
- 단일 원본 원칙
- 검증/PASS-HOLD-FAIL
- 중복·충돌 처리
- 외부 증거/read-back
- 사용자 수작업 최소화
- 공통 데이터/출력/검증 규칙

### 2) 업무군 규칙
예:
- 이메일 수집
- 고객 안내
- 7번 고객 컨택 판단
- 안내서
- 목차 정리
- 메타데이터
- 입찰
- 발행사 발굴/계약
- 기타 업무군

업무군에서 공통으로 쓰는 규칙은 개별 분야나 개별 도구에 복제하지 않고 여기에서 관리한다.

### 3) 분야/도구 예외
예:
- 반도체 / 방산 / 로봇 / 조선 / 탄소 / 바이오 / 풍력 / 원자력 / 이차전지 등 분야 고유 예외
- 1번 / 6번 / 13번 / 27번 / 34번 등 도구 고유 입출력·동작 예외

예외는 공통마스터 또는 업무군 규칙과 충돌하지 않는 범위에서만 유지한다.
충돌하면 최신의 명시적 사용자 지시를 기준으로 충돌검사 후 상위 규칙을 갱신하거나 예외를 명확히 분리한다.

### 4) 실제 데이터
- 고객장부
- 이메일 수집 결과
- 보고서 메타데이터
- 원문 목차
- 거래 기록
- 발행사 데이터
- 실행 로그/결과 파일

실제 데이터는 규칙 문서에 복제하지 않는다. 규칙은 데이터의 위치·스키마·참조방법만 가진다.

## 2. 완전 범용 재사용 원칙
- 새 도구를 만들 때 기존 규칙을 처음부터 다시 만들지 않는다.
- 새 대화창이 생겨도 공통마스터와 해당 업무군 규칙을 먼저 참조한다.
- 새 분야가 생기면 분야 고유 예외만 추가한다.
- 새 도구가 생기면 도구 고유 예외만 추가한다.
- 6번 같은 기존 도구의 엔진을 교체해도 상위 규칙과 데이터 구조는 그대로 재사용한다.
- 현재 도구와 미래 도구가 같은 규칙·검증·데이터 계약을 공유하는 것을 기본으로 한다.
- 완성된 엔진/모듈/검증규칙/출력계약은 재사용 자산으로 승격한다.
- 새 도구는 먼저 기존 재사용 자산을 검색하고, 없는 부분만 새로 만든다.

## 3. 피드백 실시간 흡수 구조
각 대화창·도구에서 사용자가 지적하거나 새로 확정한 보완사항은 해당 창에만 남겨두지 않는다.

흐름:
`대화창/도구 피드백 → 중앙 피드백 수집 → 기존 규칙 충돌검사 → 공통/업무군/분야·도구 예외 분류 → 중앙 원본 반영 → 다른 대화창·도구에서 재사용`

분류 기준:
- 여러 업무군에 적용 가능 → 공통마스터
- 같은 업무군 전체에 적용 → 업무군 규칙
- 특정 산업/분야에만 적용 → 분야 예외
- 특정 도구의 입출력/동작에만 적용 → 도구 예외
- 특정 고객/특정 건에만 적용 → 실제 데이터/사례로 보존, 공통화 금지

### 피드백 레코드 필수 항목
- 피드백 출처
- 발생 작업/도구/대화창
- 문제 또는 불편 내용
- 반복 발생 여부
- 사용자 부담 증가 여부
- 수익 영향
- 운영 영향
- 보안 영향
- 기존 규칙과 중복/충돌 여부
- 적용 범위: 공통/업무군/분야/도구/개별사례
- 우선순위
- 즉시 반영 필요 여부
- 임시 STOP 필요 여부
- 처리 유형
- 실제 반영 위치
- 반영 commit/read-back 증거

### 처리 유형
- 즉시 수정
- 묶음 통합 반영
- 보류
- 제거 후보
- 반영 금지
- 수익화 후보
- 글로벌 확장 후보

피드백을 이유로 기능을 무한 확장하지 않는다. 현재 목적과 계약된 업무범위 안에서만 반영한다.

## 4. 충돌 방지
- 새 피드백을 바로 덧붙여 규칙 수를 늘리는 방식 금지.
- 먼저 기존 규칙과 같은 의미인지 확인한다.
- 같은 의미면 기존 조항을 갱신한다.
- 충돌하면 최신 사용자 지시와 실제 업무 목적을 기준으로 하나로 통합한다.
- 공통 규칙과 예외 규칙의 경계를 명시한다.
- 이미 PASS된 규칙을 매번 재검증하지 않는다. 변경된 부분과 충돌 가능 부분을 우선 검사한다.
- 신규 보완만 별도 PATCH 파일로 계속 쌓는 방식 금지.
- 작은 보완은 중앙 후보 로그에 모으고, 동일 영역의 관련 보완을 가능한 한 한 번에 통합한다.
- 단, 현재 업무를 막는 긴급 보완은 즉시 반영한 뒤 중앙 원본에 병합한다.

## 5. 통합 속도 최우선 원칙
이 구조를 만드는 목적 중 하나는 통합 작업이 너무 느려 업무가 멈추는 문제를 없애는 것이다.

LOCK:
- 피드백은 가능한 즉시 중앙 수집함에 기록한다.
- 공통화 가능성이 명확하면 같은 작업 흐름 안에서 충돌검사 후 상위 규칙으로 승격한다.
- 사용자가 같은 보완사항을 여러 대화창에 다시 붙이지 않는다.
- 한 분야/한 도구만 먼저 완성하고 나머지를 오래 대기시키는 방식보다, 공통구조를 먼저 만들고 각 도구가 공통구조를 참조하게 하는 방식을 우선한다.
- 통합은 문서 작성 자체가 목적이 아니라 현재 업무 처리 속도와 미래 도구 재사용 속도를 높이는 것이 목적이다.
- 가능한 작업은 설계 + 산출물 + 검증 + 다음 단계까지 묶음 처리한다.
- 설명만 하고 산출물 없이 정지하는 구조를 금지한다.
- 같은 성격의 규칙 회수/분류/충돌검사는 병렬 또는 묶음으로 처리한다.

## 6. 중단 감지·자동 복구·재개 구조
통합이 느린지 멈춘 것인지 사용자가 추측하게 하지 않는다.

### REPEAT_PROMPT_BLOCK
사용자가 `진행`, `계속`, `왜 멈췄어`, `지금 하는 중이야?`, `빨리 진행`을 반복하게 되면 구조 실패 신호로 판정한다.
기록 항목:
- 반복 발생 원인
- 산출물 누락 여부
- 자동 진행 가능 여부
- 중간 정지 원인
- 구조 수정 필요 여부
- 다음 자동 처리

### AUTO_RECOVERY_LOOP
- 같은 오류 1회 → 자동 복구 시도
- 같은 오류 2회 → 대체 후보 시도
- 같은 오류 3회 → CHRONIC_FAIL 격리
- 격리 후 기존 안정 구간은 유지
- 오류 위치/기대값/실제값/원인/재시도/롤백/대체후보/다음조치를 기록

### STATE_PACKET / 재개 지점
도구·대화창·작업이 중단될 때 최소 아래 상태를 남긴다.
- 현재 PASS 구간
- 현재 FAIL/HOLD 구간
- 마지막 성공 작업
- 현재 STOP 이유
- 다음 실행 작업
- 필요한 파일/데이터
- 롤백 기준
- 다음 자동 실행 조건

사용자가 같은 설명을 다시 하지 않아도 재개 가능해야 한다.

## 7. 관찰자 보고 구조
사용자는 통합 작업이 진행 중인지 멈췄는지 직접 추측하지 않는다.

[OBSERVER_REPORT]
- 현재 상태
- 실제 완료된 것
- 자동 실행된 것
- 자동 대체된 것
- 현재 막힌 것
- 마지막 외부 증거/commit/read-back
- 다음 작업
- 사용자 판단이 필요한 단일 항목(있는 경우만)
- 아무것도 안 하면 유지되는 기본값
- 다음 자동 실행 조건

긴 내부 로그 대신 위 상태를 짧게 보고한다.
실제 증거가 없으면 `진행 중` 또는 `완료`라고 과장하지 않는다.

## 8. 주간/야간 및 승인 등급
사용자 개입 없이 가능한 작업은 승인 대기로 세우지 않는다.

A등급 자동 가능:
- 규칙 회수/충돌검사
- 정적·동적 검증
- 링크/DOM/해시/로그 분석
- evidence 생성
- 백업
- STATE_PACKET 갱신
- HOLD 큐 정리
- 실패 원인 분류
- 다음 작업 후보 작성

B등급 사용자 승인 필요:
- 구조 대변경
- 외부 서비스 연결
- 원본 교체
- 고객 발송
- 보안/권한 관련 작업

C등급 결제 승인 필요:
- 유료 플랜/유료 서버/유료 검증서비스

D등급 금지:
- 승인 없는 결제
- 비밀번호 요구
- 고객자료 무단 외부 전송
- 원본 삭제
- 샘플을 실제 결과처럼 출력
- PASS 근거 없는 완료 보고

## 9. 13일 이전 우선순위
- 개별 도구 세부기능 완성보다 범용 통합 구조를 우선한다.
- 공통마스터·업무군·예외·데이터 경계를 먼저 고정한다.
- 이미 존재하는 대화기록·문서·GitHub 규칙을 회수해 중복을 줄인다.
- 사용자 피드백이 생기는 즉시 중앙 구조에 흡수한다.
- 이후 6번 및 다른 기존/신규 도구는 이 구조를 참조해 기능을 안정화·교체·확장한다.
- 13일 이전에는 개별 UI 외형 개선보다 공통 규칙·상태·복구·검증·데이터 계약의 재사용 구조를 우선한다.

## 10. 완료 판정
완전 통합 PASS는 다음을 모두 만족해야 한다.
- 공통마스터가 단일 원본으로 존재
- 각 업무군 규칙이 공통마스터를 참조
- 분야/도구 예외가 별도로 구분
- 실제 데이터가 규칙과 분리
- 대화창/도구 피드백이 중앙 수집·충돌검사·승격 경로를 가짐
- 개별 도구가 상위 구조를 실제로 읽거나 참조하는 증거가 있음
- 새 도구/새 분야 추가 시 기존 규칙 복제가 필요 없음
- 중단 시 STATE_PACKET으로 재개 가능
- 반복 오류에 AUTO_RECOVERY_LOOP가 적용됨
- OBSERVER_REPORT로 실제 진행/정지 상태를 확인 가능

이 문서는 구조 정의 원본이며, 세부 업무 규칙은 `WIC_GLOBAL_OPERATING_RULES.md`, 고객업무는 `CUSTOMER_WORKFLOW_MASTER.md` 등 각 업무군 원본에서 관리한다.
