# TOOL020 MASTER — 20번 운영 매뉴얼 디스플레이

상태: COMPLETE / REMOTE_VERIFIED / DEPLOYED FOUNDATION
기준일: 2026-08-30

이 문서는 20번 도구 고유 규칙만 보관한다. 이 저장소의 `WIC_GLOBAL_OPERATING_RULES.md`와 `feedback_pipeline/`은 WIC 중앙 운영영역이며 TOOL020 화면 기능과 구분한다.

## 1. 현재 역할
- 각 TOOL의 사용법·주의사항·현재 기능·HOLD 사항 등 운영 매뉴얼을 블록 단위로 저장·검색·조회하는 전용 화면이다.
- 현재 검증된 1차 기반은 `저장 → 검색 → 미리보기/조회 → 로그/상태 반영`이다.
- 사용자용 공개 경로는 기존 GitHub Pages의 `/tool020/`이며, 로컬 배포본도 별도 실행 가능 상태로 유지한다.

## 2. 현재 사용가치 판단
- 사용자가 매뉴얼 내용을 일일이 직접 작성해서 저장하는 것만으로는 도구 가치가 낮다.
- 현재 수동 저장/검색 기능은 **자동 매뉴얼 생성·업데이트를 위한 기반**으로 본다.
- 현재 COMPLETE/REMOTE_VERIFIED 기반을 폐기하거나 재개발하지 않는다.

## 3. 향후 핵심 가치 — HOLD_FOR_REUSE
`TOOL020_AUTO_MANUAL_GENERATION_UPDATE = HOLD_FOR_REUSE`

목표:
- 실제 TOOL 개발·수정·검증·배포에서 나온 PASS/REMOTE_VERIFIED 증거를 입력으로 받는다.
- 각 TOOL의 사용법, 현재 가능한 기능, 주의점, HOLD/재개조건을 자동으로 생성/갱신한다.
- 사용자가 `6번 사용법`, `14번 아직 안 되는 기능`처럼 검색하면 최신 정본을 찾을 수 있게 한다.
- PASS/REMOTE_VERIFIED와 HOLD/BLOCKED를 명확히 구분한다.

재개조건:
- 다른 실제 TOOL 작업에서 `상태/변경 이벤트 → 검증된 문서 자동생성·업데이트` 컴포넌트가 PASS되거나, 사용자가 20번을 명시적으로 우선 개발 대상으로 지정한다.
- 이를 위해 별도 대규모 공통화 프로젝트나 WIC 전수조사를 시작하지 않는다.

## 4. 운영 원칙
- TOOL별 최신 canonical/checkpoint를 재사용하고 사용자가 과거 설명을 다시 입력하게 하지 않는다.
- 매뉴얼 내용은 실제 검증된 기능과 상태를 기준으로 한다.
- `예정`, `목표`, `가능할 것`을 현재 사용법처럼 기록하지 않는다.
- HOLD/BLOCKED 기능은 이유와 재개조건을 함께 표시한다.
- 같은 TOOL의 매뉴얼 버전을 파일로 계속 복제하지 않고 하나의 최신 정본을 갱신한다.

## 5. 보안 범위
- 과거 초기 대화의 `로그인 아이디·비밀번호 저장` 아이디어는 현재 핵심 역할에서 제외한다.
- 운영 매뉴얼 도구에 실제 비밀번호/비밀키를 일반 텍스트나 localStorage로 저장하는 기능을 기본 요구사항으로 복원하지 않는다.
- 자격증명 관리는 별도의 안전한 비밀관리 수단을 사용해야 하며 매뉴얼 검색 기능과 섞지 않는다.

## 6. 검증·재작업 방지
- 현재 저장/검색/조회 기반의 기존 PASS 범위는 SKIP_REUSE한다.
- 새 자동생성/업데이트 기능이 추가될 때 해당 변경 범위만 FIRST_VALIDATION 1회 수행한다.
- 기존 Pages 배포와 정상 기반을 catch-up 때문에 재시험하지 않는다.
- 실제 evidence 없이 `자동 갱신된다`고 표시하지 않는다.

## 7. 2026-08-30 대화기록 catch-up
- 과거 20번 기록의 `모든 운영 매뉴얼 관리` 목적은 유지하되 초기 비밀번호 저장·가상 진행률·자동 백그라운드 개발 주장은 현재 요구사항/상태로 복원하지 않는다.
- 최신 사용자 피드백에 따라 `수동 매뉴얼 저장만으로는 가치 낮음 → 실제 TOOL 증거 기반 자동생성·지속업데이트가 핵심 확장`으로 우선순위를 갱신했다.
- 이후 20번 대화창에서 `업데이트` 입력 시 신규 영구 피드백만 DIFF ONLY 반영한다.
- 실제 GitHub write/commit/read-back 없이 업데이트 완료라고 보고하지 않는다.
