# WIC 앞뒤 동시 압축 실행 규칙 — 2026-09-15

상태: CHAT_COMPRESSION_ADDENDUM = COMPLETE
기준 원본: `feedback_pipeline/WIC_CHAT_FULL_COMPRESSION_20260915.md`

## 핵심 변경
기존 1~13회차와 WORK-BATCH A~D는 **실행 순서가 아니다.** 검사할 내용을 나눠 적은 번호다.

금지:
- 1 끝난 뒤 2 시작
- A 끝난 뒤 B 시작
- 한 회차씩 끝내고 사용자에게 `계속` 요구
- 이미 끝난 PASS를 다시 조사/시험
- 같은 ROOT를 여러 도구에서 따로 고침

실행 방식:
`전체 1~13 동시 펼침 → 같은 ROOT/같은 증거/같은 부품끼리 합침 → 서로 기다릴 필요 없는 작업 동시 처리 → 이미 검증된 것은 즉시 SKIP_REUSE → 꼭 앞 결과가 필요한 부분만 짧게 대기 → 전체 ZERO SCAN → 빠진 것만 다시 동시 처리`

## 동시에 묶을 큰 축
### 축 1 — 16↔44 + 23건 + 장착/복구
1·2·3·4회차를 따로 하지 않는다.
- 원본 23 demand 분류
- 기존 검증부품 재사용
- 진짜 missing만 외부 완성부품 확인
- 24시간 막힘은 외부 재검색에만 적용
- 장착/실패원복/결과귀환/remote read-back을 가능한 건 같은 흐름에서 같이 검증

### 축 2 — 과거오류 + 오늘오류 + 앞으로 오류
5·6·11회차를 같이 처리한다.
- TOOL045 283 / 171,910 / 315 기존 정본 재사용
- 재추출 금지
- TOOL016 ledger + 오늘 오류 + 신규 이벤트를 같은 ROOT로 합침
- 오래된 HOLD/중복/잘못된 완료표시도 동시에 정리
- source-side producer처럼 플랫폼상 불가능한 것은 별도 HOLD

### 축 3 — 실제 업무도구 고질오류
7·8·9·10회차를 같이 처리한다.
- TOOL041/042/007/034 고객업무
- TOOL006 TOC
- TOOL013 Excel/재개/중복/번역
- 다른 도구의 같은 수작업·재설명 문제
- 새 대화창 상태복구
같은 원인이면 도구별로 따로 만들지 않고 ROOT 하나로 묶어 고친다.

### 축 4 — 실제 기기/화면 + 최종검사
12·13회차는 다른 축이 끝날 때까지 놀고 기다리는 마지막 단계가 아니다.
- 실제 기기 증거가 없는 항목은 즉시 정확한 HOLD로 분리
- 내부에서 확인 가능한 화면/상태/연결은 앞 축과 동시에 검사
- ZERO SCAN은 중간에도 계속 돌리고 마지막에 한 번 더 돌림

## 기다림 규칙
어떤 작업이 다른 결과를 꼭 필요로 할 때만 기다린다.
그동안 다른 축은 계속 진행한다.
한 항목의 실패가 전체를 멈추게 하지 않는다.

## 사용자 규칙
USER_WORK=0
USER_MANUAL_RELAY=0
USER_CONTINUE_REQUIRED=0
USER_REEXPLAIN_REQUIRED=0

사용자는 관찰자다. 중간 승인·복사·붙여넣기·재설명·수동 전달을 요구하지 않는다.

## 완료 규칙
코드/문서/후보 발견만으로 완료 금지.
가능한 항목은 `실제 실행 → 시험 → 실패 시 수정/원복 → 재시험 → 저장 → GitHub 다시 읽기`까지 끝낸다.
외부 기기·권한·실제 자료가 없어서 못 하는 것은 기다리며 전체를 막지 않고 이유가 적힌 HOLD로 남긴다.

## 시간 압축 원칙
어제 사용한 방식 그대로 앞뒤 작업을 겹친다.
따라서 A→B→C→D 시간을 단순 합산하지 않는다.
현재 목표 실행창은 **30~60분짜리 큰 병렬 실행**이며, 그 뒤 ZERO SCAN에서 실제 빠진 것만 다시 묶어 처리한다.

## 다음 실행점
새 조사계획을 다시 만들지 않는다.
이 문서와 `WIC_CHAT_FULL_COMPRESSION_20260915.md`를 입력으로 삼아 실제 남은 DIFF만 동시에 실행한다.
