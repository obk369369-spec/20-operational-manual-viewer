# WIC 공통 실행 규칙 — 2026-09-22

적용범위: WIC 전체 도구/대화창/고객업무/자동화/자료처리/파일작업
우선목표: 사용자는 관찰자. 반복 지시와 중간 개입을 최소화하고 실제 완료 결과를 만든다.

## 1. CONTINUE_100 규칙
- 사용자의 `계속`은 별도 비율 지시가 없어도 `계속 100%`로 해석한다.
- 한 응답/한 실행에서 가능한 최대 작업량을 실제 처리하고, 가능하면 100% 완료까지 간다.
- 30%, 50% 등 중간 진행보고를 하기 위해 작업을 조기 종료하지 않는다.
- 실행 한도/외부 도구 한도 때문에 100%가 불가능한 경우에만 실제 완료 지점과 남은 작업을 기록한다.
- 다음 `계속`에서는 남은 전량을 다시 100% 완료 목표로 처리한다.
- 진행률 숫자만 올리는 행위 금지. 조사·실행·검증이 실제 끝난 양만 진행률에 반영한다.

## 2. OBSERVER 규칙
- 사용자는 관찰자다.
- 중간 승인, 반복 확인, 동일 자료 재첨부, 수동 비교·검증을 사용자에게 요구하지 않는다.
- 기존 대화/파일/checkpoint/규칙/결과에서 회수 가능한 정보는 먼저 직접 회수한다.
- 중간 상태보고보다 실제 실행을 우선한다.

## 3. 44번/16번 순환 목표
- 44번 외부 무인 순환구조 완성 전에도 각 `계속` 실행을 최대 배치로 처리한다.
- 완성 후 목표는 USER_CONTINUE_DEPENDENCY=0 이다.
- 최초 지시 후 외부 실행층이 queue → checkpoint → retry → validation → completion을 24시간 이어간다.
- 오류/HOLD/FAIL/신규 운영규칙은 16번에서 ROOT/atomic demand로 정리하고 44번의 외부 검증부품 탐색·시험·배포 구조와 연결한다.
- 무료/저가 실행 우선. 유료 API 키 의존을 기본 해결책으로 삼지 않는다.

## 4. 증거 기반 PASS
- 파일명, 로그, META, JSON, 전달문, 진행률만으로 PASS 금지.
- 실제 결과물/실행/read-back/검증/회귀검사 등 필요한 증거가 있어야 PASS.
- 미확정은 HOLD, 실패는 FAIL. 사실을 만들지 않는다.

## 5. 고객 통합 작업 공통 규칙
- 장부 + 온라인 + 과거 메일/견적/구매/발송 이력을 교차검증한다.
- 동일 고객은 한 행/하나의 영구 ID로 병합한다.
- 기존 CUSTOMER_ID를 우선 회수·재사용한다.
- ID 충돌은 ID_CONFLICT, 전수 확인 후에도 기존 ID가 확인되지 않으면 ID_UNRESOLVED. 임의 번호 생성 금지.
- 현재 재직/연락처와 과거 이력을 분리한다. 과거 연락처를 현재값으로 가장하지 않는다.
- 현재 근거 부족 시 HOLD.
- 과거 발송자료와 기존 완성 안내서를 먼저 대조하고 동일 발송은 SKIP한다.

## 6. 파일/버전 관리
- 작업본과 VERIFIED 기준본을 구분한다.
- VERIFIED 생성 후 이전 WORKING 파일을 현 기준본처럼 사용하지 않는다.
- 파일명은 분야/날짜/버전/상태를 식별 가능하게 관리한다.
- '최종/진짜최종/최신본' 식의 임의 파일명 반복을 피한다.
- 새 대화창에서도 기존 checkpoint와 기준본을 먼저 회수하고 사용자에게 재설명을 요구하지 않는다.

## 7. 2026-09-22 섬유 고객 기준본
- 현재 기준본: WIC_고객마스터_섬유_20260922_VERIFIED_v002.xlsx
- 이전 v001은 폐기 대상이며 현 기준본으로 사용하지 않는다.
- 강승구(CAR-019)는 2026-09-22 발송 완료 이력으로 동일 세트 재발송 금지.

## 8. 완료 보고
- 실제 저장/업데이트/read-back이 끝난 뒤에만 '업데이트 완료'라고 보고한다.
- 단순 정리·설명·예정 상태를 업데이트 완료라고 표현하지 않는다.
