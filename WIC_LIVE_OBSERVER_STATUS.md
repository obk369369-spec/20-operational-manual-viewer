# WIC LIVE OBSERVER STATUS

최종 확인: 2026-08-10 01:48 KST
상태: ACTIVE — 1번 안내서 과거 규칙/E2E 증거 대조 완료, 실행본 저장소 HOLD

이 파일은 사용자가 직접 테스트하지 않고 야간 진행을 관찰하기 위한 외부 상태판이다. 실제 외부 증거가 있을 때만 진행으로 기록한다.

## 이번 회차 실제 작업 — 01:48
- 직전 재시작 지점에서 중앙원본 안전 병합 수단 문제를 재확인하고, 원본 손실 위험이 있으므로 긴 `WIC_GLOBAL_OPERATING_RULES.md` 전체 덮어쓰기는 계속 보류했다.
- 우선순위 규칙에 따라 즉시 1번 고객 자동화 안내서로 넘어가 File Library의 과거 원문을 재검색했다.
- `1번 고객 자동화 안내서 12.doc` 계열에서 최종 완료 게이트를 회수했다: 왼쪽 입력→가운데 9칸 정상 생성, 가운데→오른쪽 안내서 100% 반영, 공급가격 포함 출력, 값 꼬임 없음, 입력 변경 시 즉시 결과 변경.
- 같은 원문에서 절대금지를 회수했다: 오른쪽 안내서 구조 임의수정 금지, trace/표식/디버그 재삽입 금지, 왼쪽→오른쪽 직결 금지, 임시 하드코딩 금지, 한 번에 전체수정 금지.
- 작업 순서 잠금도 확인했다: 매핑표→제목→도서정보→링크→목차→자동연결→안정화→표시정리.
- 다른 1번 원문에서 현재 범위가 `자동발송`이 아니라 `발송 직전 완성형 생성`임을 확인했다. 고객 입력→자료 5개 매칭→메일 5개(제목+본문) 생성→스팸회피까지이며 실제 메일 전송은 제외한다.
- 과거 코드 원문에서 5개 안내서 탭, 가운데 필드(title EN/KO, publisher/date/page/list price/supply/link/TOC), 오른쪽 안내서 렌더 구조가 실제 HTML/JS로 존재했던 증거를 확인했다.
- 그러나 GitHub 조직 검색에서 과거 실행본 식별자인 `P4_EXCLUDE_FILTER_FINAL_V6`를 찾지 못했다. 따라서 현재 GitHub에 배포/보존된 실제 1번 실행본을 확인하지 않은 상태에서 E2E PASS를 주지 않았다.

## 이번 회차 판정
| 작업 묶음 | 상태 | 외부 증거 | blocker / 개선 | 다음 실행 |
|---|---|---|---|---|
| 이메일 수집/고객 DB 중앙대조 | PASS(규칙 대조) | 이전 회차 File Library + 중앙 §8 read-back | 실행 DB 자체 검증은 별도 | 누락 규칙만 계속 선별 |
| 7번 고객 컨택 중앙대조 | PASS(규칙 대조) / 병합 HOLD | 이전 회차 과거 원문 + 중앙 §9 read-back | 중앙 긴 파일 안전 patch 수단 부족 | 원문 손실 없는 쓰기 가능 시 병합 |
| 1번 안내서 규칙 회수 | PASS | File Library `1번 고객 자동화 안내서 12.doc`, `10.doc`, `21.txt` 계열 | 없음 | 현재 실행본과 규칙 대조 |
| 1번 과거 구현 증거 | PASS(과거 코드 존재) | 과거 HTML/JS 원문에서 5탭·중간필드·오른쪽 렌더 확인 | 과거 코드가 현재 실행본이라는 보장은 없음 | 현재 저장소/배포본 식별 |
| 1번 현재 E2E | HOLD | GitHub org 검색에서 `P4_EXCLUDE_FILTER_FINAL_V6` 미발견 | 실제 실행본 저장소/배포 위치가 현재 증거로 식별되지 않음 | GitHub/File Library에서 최신 파일명·저장소 재탐색 후 실제 입력→출력 검증 |
| 중앙 운영원본 실제 쓰기 | HOLD(안전) | 긴 파일 fetch 절단 이력 | 전체 덮어쓰기 시 기존 규칙 손실 위험 | 안전 patch/전체원문 확보 전 쓰기 금지 |
| 37번 메타데이터 | ACTIVE 규칙정리 | 번호 분리 LOCK 유지 | 13번과 별도 | 1번 다음 우선순위로 독립 실행 게이트 정리 |
| 13번 엑셀 자동 업로드 | HOLD(회귀검증) | 번호 분리 LOCK 유지 | 날짜/업로드 회귀 미검증 | 37 완료 뒤 독립 검증 |

## 1번 현재 잠금 규칙
- 5개 안내서 구조를 먼저 유지하고 슬롯/가운데 매핑을 통해 오른쪽에 반영한다.
- 왼쪽 입력을 오른쪽에 직접 연결하지 않는다.
- 출력은 실제 고객/실제 보고서 데이터 기반이어야 하며 샘플·가짜 보고서를 실제 결과처럼 PASS 처리하지 않는다.
- 자동 메일 발송은 현재 범위 밖이다. 발송 직전 복붙 가능한 5세트 생성까지가 범위다.
- 최종 PASS는 실제 입력→가운데 매핑→오른쪽 5개 안내서/메일 결과→예상값 비교를 실행한 증거가 있어야 한다.

## 번호 혼동 금지
- 37번 = 메타데이터 생산·한글 타이틀·필수칼럼·원본/결과 검증
- 13번 = 엑셀 자동 업로드 도구
- `37/13 메타데이터`처럼 묶어서 표현하지 않는다.

## PASS 기준
- GitHub 저장: write 응답 + read-back/SHA
- 기능: 실제 입력 → 실제 처리 → 실제 출력 → 예상값 비교
- 웹/자료 검증: 공식 출처와 결과 일치
- 증거가 부족하면 HOLD

## 사용자 작업 금지
사용자는 이 상태판을 보기만 한다. 테스트, 비교, 캡처, PASS/FAIL 판정, 규칙 저장, 새 대화창 인계문 작성, 정지 여부 감시, `계속/진행` 반복 입력을 요구하지 않는다.

## 재시작 지점
다음 실행은 1번의 최신 실행 HTML/저장소/배포 위치를 GitHub와 File Library에서 식별하는 작업부터 재개한다. 식별되면 과거 잠금 규칙과 직접 대조하고 실제 E2E 가능 여부를 판정한다. 식별되지 않으면 HOLD 원인을 고정하고 즉시 37번 메타데이터 독립 실행 게이트 회수·대조로 넘어간다.

실행시간: duration not exposed
