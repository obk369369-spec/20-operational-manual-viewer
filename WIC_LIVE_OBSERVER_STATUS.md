# WIC LIVE OBSERVER STATUS

최종 확인: 2026-08-10 03:51 KST
상태: ACTIVE — 37번 규칙 충돌 판정 + 13번 날짜 serial 회귀 원인 회수

이 파일은 사용자가 직접 테스트하지 않고 야간 진행을 관찰하기 위한 외부 상태판이다. 실제 외부 증거가 있을 때만 진행으로 기록한다.

## 이번 회차 실제 작업 — 03:51
- 직전 재시작 지점에서 37번 회수 게이트를 기존 통합운영 문서/통합규칙관리와 대조했다.
- 37번 최신 잠금은 `상품명=원본 영문 제목의 한국어 번역`, `한글명=원본 영문 제목`이다. 과거 통합운영 문서의 Allied/AnalystView `상품명은 번역 전까지 비움` 문구는 최신 잠금과 충돌하므로 현재 실행 규칙으로 승계하지 않는다.
- 37번 과거 문서에는 `13번 도구=신규 발행사 규칙 확정용`이라는 오래된 역할 정의가 남아 있음을 확인했다. 현재 번호 잠금은 `13번=엑셀 자동 업로드 도구`이므로 이 과거 정의는 DEPRECATED 충돌로 판정한다.
- 37번 18개 홈페이지 컬럼, 전체행 검사, 원본/출력 행수 일치, 발행사 전행, 실제 날짜값, 카테고리/개요/목차/가격/체제/코드 검증은 최신 회수 게이트와 정합한다.
- 중앙 `WIC_GLOBAL_OPERATING_RULES.md`는 GitHub에서 직접 fetch했으나 응답이 긴 파일 중간에서 절단되는 것을 재확인했다. 전체 원문을 보존한 안전한 부분 patch 수단이 현재 노출되지 않아 중앙원본 덮어쓰기는 계속 HOLD했다. 기존 내용을 잃을 수 있는 전체 replace는 하지 않았다.
- 즉시 13번 엑셀 자동 업로드 회귀 증거로 이동했다. 과거 검증 원문에서 `13번_LOCKED_PASS_ALL_업로드이식완료본.html`의 발행일 원본연결 경로가 46145를 그대로 반환하는 FAIL을 회수했다.
- 13번 직접 원인은 `applyField(field,row)`에서 `cfg.mode === 원본연결`일 때 sourceValue를 조기 반환하여 `normalizePublishDate()`를 우회하는 것이다.
- 회귀 수정 게이트는 field가 `발행일`이면 원본연결/preset 여부와 무관하게 최종 반환 전 `normalizePublishDate()`를 통과시키는 것. 기존 PASS인 체제 pages, 목차/개요 줄바꿈, 패널, 다운로드 구조는 수정 금지다.
- 과거 검증에서 preset 엑셀 날짜 해석, preset YYYY-MM-DD, 2026 단독 연도 유지, 46145 serial 변환 자체, 체제 pages는 PASS였으므로 수정 범위를 발행일 원본연결 경로로 제한한다.

## 이번 회차 판정
| 작업 묶음 | 상태 | 외부 증거 | blocker / 개선 | 다음 실행 |
|---|---|---|---|---|
| 37번 최신 제목 잠금 대조 | PASS(규칙) | File Library `37번_메타데이터_상품명_ISBNCODE_누락방지_잠금보완사항.txt` | 없음 | 최신 잠금 우선 유지 |
| 37번 과거 상품명 빈칸 규칙 | DEPRECATED(충돌) | 통합운영 문서의 Allied/AnalystView 과거 규칙 | 최신 상품명 필수 한글번역과 충돌 | 실행 규칙에서 제외 |
| 37번 과거 13번 역할 정의 | DEPRECATED(충돌) | `37번_메타데이터_통합규칙관리.xlsx` 변경이력/운영문서 | 현재 번호 잠금과 충돌 | 13번=엑셀 자동 업로드만 유지 |
| 중앙 운영원본 실제 쓰기 | HOLD(안전) | GitHub `WIC_GLOBAL_OPERATING_RULES.md` fetch가 중간 절단됨 | 전체 replace 시 규칙 손실 위험 | 안전 patch/전체원문 확보 전 쓰기 금지 |
| 13번 46145 회귀 원인 회수 | PASS(원인확정) | 과거 33번 검증 원문 read-back | 현재 수정본/실행본 미확인 | 최신 13번 실행본 위치 및 수정 반영 여부 확인 |
| 13번 현재 기능 | HOLD(회귀검증) | 원본연결 경로 FAIL 증거 존재 | 수정본의 실제 E2E 증거 없음 | 최신 파일 탐색→코드 확인→가능하면 회귀검증 |

## 13번 회귀검증 잠금
1. 발행일 원본연결 + Publishing Date=46145가 그대로 출력되면 FAIL.
2. `field === 발행일`은 mode/preset과 무관하게 최종 출력 전 `normalizePublishDate()` 통과.
3. preset 엑셀 날짜 해석과 YYYY-MM-DD 기존 PASS 유지.
4. `2026` 같은 4자리 연도를 1905 날짜 등으로 오변환하지 않는다.
5. 체제 `페이지수+pages`, 목차/개요 줄바꿈, 패널, trace/upload 다운로드 기존 PASS 구조는 건드리지 않는다.
6. 실제 수정본에서 미리보기와 업로드 다운로드 결과가 일치해야 최종 PASS.

## 번호 혼동 금지
- 37번 = 메타데이터 생산·한글 타이틀·필수칼럼·원본/결과 검증
- 13번 = 엑셀 자동 업로드 도구
- 과거 문서의 `13번=신규 발행사 규칙 확정용` 정의는 현재 DEPRECATED.
- `37/13 메타데이터`처럼 묶어서 표현하지 않는다.

## PASS 기준
- GitHub 저장: write 응답 + read-back/SHA
- 기능: 실제 입력 → 실제 처리 → 실제 출력 → 예상값 비교
- 웹/자료 검증: 공식 출처와 결과 일치
- 증거가 부족하면 HOLD

## 사용자 작업 금지
사용자는 이 상태판을 보기만 한다. 테스트, 비교, 캡처, PASS/FAIL 판정, 규칙 저장, 새 대화창 인계문 작성, 정지 여부 감시, `계속/진행` 반복 입력을 요구하지 않는다.

## 재시작 지점
다음 실행은 13번 최신 HTML/실행 저장소를 File Library와 GitHub에서 찾아 `applyField()` 발행일 원본연결 경로의 수정 반영 여부를 확인한다. 실행 가능한 최신본을 확보하면 46145/2026/preset/trace-upload 회귀검증으로 이어간다. 현재본을 찾지 못하면 정확한 HOLD 원인을 남기고 즉시 6번 목차 정리의 최신 안정본 v2.26 및 자동진단/의심줄 규칙 검증으로 넘어간다.

실행시간: duration not exposed
